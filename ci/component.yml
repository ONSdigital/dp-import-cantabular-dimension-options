---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: docker/compose
    tag: latest
    privileged: true

inputs:
  - name: dp-import-cantabular-dimension-options

run:
  path: dp-import-cantabular-dimension-options/ci/scripts/component.sh

caches:
  - path: /var/run/docker.sock
